{
  "name": "web3stick",
  "private": true,
  "version": "0.0.0",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "dev": "parcel",
    "build": "parcel build",
    "dist": "parcel build",
    "clean": "rm -rf dist",
    "web4_testnet": "web4-deploy dist web4.web3stick.testnet --nearfs",
    "web4_mainnet": "web4-deploy dist web4.web3stick.near --nearfs"
  },
  "dependencies": {},
  "devDependencies": {
    "parcel": "^2.14.0",
    "web4-deploy": "^0.8.0"
  }
}
