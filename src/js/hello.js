// Function to log "Hello" to the console
function logHello() {
    console.log("👋 Hello, Welcome to Web3Stick");
}

// Call the function when DOM is ready
document.addEventListener('DOMContentLoaded', logHello);